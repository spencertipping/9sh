#pragma once
#include <algorithm>
#include <cassert>
#include <cmath>
#include <cstdint>
#include <cstring>
#include <iostream>
#include <memory>
#include <span>
#include <string>
#include <vector>

using u8  = uint8_t;
using u8c = uint8_t const;
using uN  = size_t;
using iN  = ssize_t;

using ch  = char;
using chc = char const;
using St  = std::string;
using Stc = std::string const;

#define let auto const
#define A(x) assert(x)
